<%= form_with(model: subcategory, local: true) do |form| %>
  <% if subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="text-align: left">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :subcategory_category_id, class: "form-control" %>
  </div>

  <div class="field" style="text-align: left">
    <%= form.label :title, 'Título' %>
    <%= form.text_field :title, id: :subcategory_title, class: "form-control" %>
  </div>

  <div class="field" style="text-align: left">
    <%= form.label :desc,'Descripción' %>
    <%= form.text_area :desc, id: :subcategory_desc, class: "form-control" %>
  </div>
  <br>
  <div class="field">
    <%= form.submit "Guardar", class: "btn btn-info" %>
  </div>
<% end %>
